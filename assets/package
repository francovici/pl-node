{
  "name": "my-app",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "split": "npx pl --split",
    "test": "mocha ./tests/**.test.js",
    "compile":"npx pl --compile",
    "build":"npx pl --build",
    "clean":"npx pl --clean",
    "help":"npx pl --help"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.1.0"
  }
}
